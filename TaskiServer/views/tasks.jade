.col-md-12(ng-controller="tasks")
  form.form-inline
    .form-group
      label(for="title") Title
      input.form-control#title(type="text", placeholder="title" ng-model="title")
    button.btn.btn-group(type="submit", ng-click="create()") Add Task
  .list-group(style="margin-top: 18px;")
    a.list-group-item(href="#modal", data-toggle="modal", ng-repeat="task in tasks") {{task.title}}
  #modal.modal.hide.fade(tabindex="-1", role="dialog", aria-labelledby="mymodalLabel" aria-hidden="true")
    div.modal-header
      button.close(type="button", data-dismiss="modal", aria-hidden="true") x
